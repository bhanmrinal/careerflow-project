# OpenAI Configuration (not used, kept for compatibility)
OPENAI_API_KEY=

# Groq Configuration (Llama 3.3 70B - Recommended)
# Get free API key at https://console.groq.com/
GROQ_API_KEY=your_groq_api_key_here

# LLM Provider Selection: groq, huggingface, or ollama
LLM_PROVIDER=groq
GROQ_MODEL=llama-3.3-70b-versatile

# HuggingFace Configuration (Optional)
HUGGINGFACE_API_KEY=
HUGGINGFACE_MODEL=meta-llama/Llama-3.2-3B-Instruct

# Ollama Configuration (Optional - for local models)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# Embedding Model
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Firebase Configuration (Optional - uses in-memory storage if not set)
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=
FIREBASE_CLIENT_ID=
FIREBASE_AUTH_URI=https://accounts.google.com/o/oauth2/auth
FIREBASE_TOKEN_URI=https://oauth2.googleapis.com/token
FIREBASE_AUTH_PROVIDER_CERT_URL=https://www.googleapis.com/oauth2/v1/certs
FIREBASE_CLIENT_CERT_URL=

# Application Settings
APP_ENV=development
DEBUG=true
SECRET_KEY=your_secret_key_change_in_production

# ChromaDB Settings
CHROMA_PERSIST_DIRECTORY=./chroma_db

# Server Settings
HOST=0.0.0.0
PORT=8000

# File Upload Settings
UPLOAD_DIRECTORY=./uploads
MAX_FILE_SIZE_MB=10
